kind = "web"
previewPath = "/"
title = "DeepFake Detector"
version = "1.0.0"
id = "artifacts/detector"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21948

[services.development]
run = "pnpm --filter @workspace/detector run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/detector", "run", "build" ]
publicDir = "artifacts/detector/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21948"
BASE_PATH = "/"
